@model Etch.OrchardCore.Leaflet.ViewModels.MapTilesViewModel

@if (!Model.ContentItem.CreatedUtc.HasValue)
{
    <div class="alert alert-warning" role="alert">@T["Tiles will be generated after creation."]</div>
}