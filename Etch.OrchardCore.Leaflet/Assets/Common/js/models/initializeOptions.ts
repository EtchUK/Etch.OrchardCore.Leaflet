export default interface IInitialiseOptions {
    domId: string;
    height: number;
    initialZoom: number;
    maxZoom: number;
    minZoom: number;
    tileRoot: string;
    width: number;
}
